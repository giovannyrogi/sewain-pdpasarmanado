# Internal application URL used by server-side cron examples.
APP_INTERNAL_URL=http://127.0.0.1:3000

# Long random secret used by POST /api/jobs/sync-expired-rooms.
# Do not expose this through NEXT_PUBLIC_*.
ROOM_SYNC_CRON_SECRET=replace_with_long_random_secret
